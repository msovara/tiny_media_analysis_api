# Minimal requirements for Lengau Cluster Job Management API
# These are the essential packages needed for core functionality

# Core FastAPI framework
fastapi>=0.104.0

# ASGI server for running FastAPI
uvicorn[standard]>=0.24.0

# Data validation and serialization
pydantic>=2.5.0

# HTTP client for testing (optional, can be removed if not needed)
requests>=2.25.0

# Note: The following packages are optional and can be installed separately if needed:
# - aiofiles: For async file operations (optional)
# - python-dotenv: For environment variable loading (optional)
# - pytest: For testing (optional)
# - pytest-asyncio: For async testing (optional)
# - httpx: For testing (optional)


































